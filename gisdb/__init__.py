"""Агентная база данных проекта прогноза золото-урановых узлов.

Пакет собирает то, без чего база не работает: подключение и пароли (`db`),
запись данных и происхождения (`db_ingest`), единая точка обращения к
языковой модели (`agent_llm`), факты о признаке для агента и для человека
(`feature_facts`), панель подтверждений (`panel`) и ссылка на репозиторий
проекта, откуда берутся сами признаки (`project`).
"""
