__pycache__/
*.pyc
.pytest_cache/

# Собираемое заново скриптами из scripts/: рисунки, схемы и сама презентация.
# В гите лежит то, из чего они собираются, а не их вес в мегабайтах.
outputs/
docs/*.pptx

# Ничего из этого в репозитории быть не должно: пароли лежат в
# F:\GIS-DB\secrets, дампы и растры — в хранилище на F:.
*.pw
*.key
*.dump
